# data/

Place the following here (not committed until you add them):

- `test.parquet` — copy from your EDA output (`eda_outputs/test.parquet`). Read by Page 2.
- `baseline.json` — generated by running `python make_baseline.py "path/to/train.parquet"`.

Page 2 will not render until both exist.
