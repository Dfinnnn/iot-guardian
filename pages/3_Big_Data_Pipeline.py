"""
Page 3 — Big Data Pipeline
Elective: Big Data & Cloud Computing
Build basis: shows PySpark preprocessing output (Parquet), pipeline diagram,
Spark code, throughput metrics. Spark runs locally; app reads Parquet output.

STUB — built in a later step (Person A).
"""
import streamlit as st

st.set_page_config(page_title="Big Data Pipeline · IoT Guardian", page_icon="🛡️", layout="wide")

st.title("3 · Big Data Pipeline")
st.caption("Big Data & Cloud Computing elective")

st.info("Coming soon — PySpark preprocessing output, pipeline diagram, and throughput metrics.")
